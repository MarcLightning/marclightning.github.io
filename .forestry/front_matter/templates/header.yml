---
label: header
hide_body: true
fields:
- name: logo
  type: file
  config:
    maxSize: 250
  label: Logo
- name: nav
  type: field_group_list
  fields:
  - name: nav_item
    type: include
    config: {}
    template: nav-item
    label: Nav Item
  config:
    min: 
    max: '5'
    labelField: 
  label: Nav
pages:
- header.md
